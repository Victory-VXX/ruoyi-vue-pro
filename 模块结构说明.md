# Ruoyi-Vue-Pro 项目模块结构说明

> 项目版本：`2026.01-SNAPSHOT` | 基于Spring Boot 3.5.9 + Java 17

本文档详细说明 Ruoyi-Vue-Pro 项目的模块结构、功能说明及依赖关系。

---

## 目录

- [模块概览](#模块概览)
- [核心模块说明](#核心模块说明)
- [Framework Starters（技术组件）](#framework-starters技术组件)
- [业务模块说明](#业务模块说明)
- [模块依赖关系](#模块依赖关系)
- [模块间通信方式](#模块间通信方式)
- [核心设计特点](#核心设计特点)
- [如何启用可选模块](#如何启用可选模块)

---

## 模块概览

### 核心模块（默认启用）

| 模块 | 功能 | 描述 |
|------|------|------|
| `yudao-dependencies` | 依赖管理 | BOM，统一管理所有依赖版本 |
| `yudao-framework` | 技术框架 | 包含14个技术组件starter |
| `yudao-server` | 主应用入口 | 聚合各业务模块，提供RESTful API |
| `yudao-module-system` | 系统管理 | 用户、权限、角色、部门、字典等 |
| `yudao-module-infra` | 基础设施 | 代码生成、文件存储、配置、WebSocket等 |

### 可选业务模块（默认注释）

| 模块 | 功能 | 描述 |
|------|------|------|
| `yudao-module-bpm` | 工作流管理 | 基于Flowable的工作流引擎 |
| `yudao-module-pay` | 支付服务 | 商户、应用、支付、退款管理 |
| `yudao-module-mall` | 商城系统 | 产品、促销、交易、统计 |
| `yudao-module-crm` | 客户关系管理 | CRM业务功能 |
| `yudao-module-erp` | 企业资源规划 | ERP业务功能 |
| `yudao-module-member` | 会员中心 | 会员管理 |
| `yudao-module-ai` | AI大模型集成 | AI功能集成 |
| `yudao-module-iot` | 物联网 | IoT设备管理 |
| `yudao-module-mp` | 微信公众号 | 微信公众号管理 |
| `yudao-module-report` | 数据报表 | 报表功能 |

---

## 核心模块说明

### 1. yudao-dependencies

**功能**：统一管理项目依赖版本（BOM - Bill of Materials）

**关键信息**：
- 版本：`2026.01-SNAPSHOT`
- 管理100+个依赖版本
- 核心依赖：Spring Boot 3.5.9, MyBatis Plus 3.5.15, Redisson 3.52.0

### 2. yudao-server

**功能**：主应用入口，作为容器聚合业务模块

**特点**：
- 默认依赖：`yudao-module-system`, `yudao-module-infra`
- 空壳项目，通过依赖引入业务模块
- 提供统一的RESTful API入口
- 主类：`YudaoServerApplication`

### 3. yudao-framework

**功能**：技术组件框架，包含14个独立的starter

**设计理念**：
- 技术组件与业务逻辑分离
- 每个starter职责单一，可独立使用
- 遵循Spring Boot自动配置机制

---

## Framework Starters（技术组件）

### 基础组件（6个）

| Starter | 功能 | 核心依赖 |
|---------|------|----------|
| `yudao-common` | 通用工具类 | 无依赖，最底层 |
| `yudao-starter-web` | Web层封装 | Spring Web, Knife4j, AOP |
| `yudao-starter-security` | 安全认证 | Spring Security, Sa-Token |
| `yudao-starter-mybatis` | 数据库访问 | MyBatis Plus, Druid, 动态数据源 |
| `yudao-starter-redis` | 缓存封装 | Redisson, Spring Cache |
| `yudao-starter-job` | 定时任务 | Quartz, Spring Async |
| `yudao-starter-mq` | 消息队列 | RocketMQ/Kafka/RabbitMQ（可选） |

**功能详解**：

- **yudao-common**：通用工具类、异常类、枚举、常量定义
- **yudao-starter-web**：
  - 全局异常处理
  - API日志记录
  - 数据脱敏
  - 错误码管理
  - Swagger/Knife4j集成

- **yudao-starter-security**：
  - 用户认证（JWT、OAuth2、微信、短信验证码）
  - 权限校验（基于Spring Security）
  - 操作日志记录

- **yudao-starter-mybatis**：
  - 多数据源支持（动态切换）
  - 事务管理
  - MyBatis Plus增强
  - SQL性能监控

- **yudao-starter-redis**：
  - Redisson分布式锁
  - 缓存抽象
  - 限流功能

- **yudao-starter-job**：
  - Quartz定时任务
  - Spring异步任务
  - 任务调度管理

- **yudao-starter-mq**：
  - 支持多种MQ（Redis Stream、RocketMQ、RabbitMQ、Kafka）
  - 消息生产者/消费者封装
  - 消息重试机制

### 业务组件（8个）

| Starter | 功能 | 核心依赖 |
|---------|------|----------|
| `yudao-starter-biz-tenant` | 多租户 | security, mybatis, redis |
| `yudao-starter-biz-data-permission` | 数据权限 | security, mybatis |
| `yudao-starter-biz-ip` | IP管理 | security |
| `yudao-starter-monitor` | 监控 | Spring Boot Actuator |
| `yudao-starter-protection` | 保护组件 | 限流、防攻击 |
| `yudao-starter-excel` | Excel处理 | EasyExcel |
| `yudao-starter-test` | 测试工具 | JUnit, Mockito |
| `yudao-starter-websocket` | WebSocket | Spring WebSocket |

**功能详解**：

- **yudao-starter-biz-tenant**：
  - 基于tenant_id的多租户隔离
  - 自动传递租户上下文
  - 数据库层面过滤

- **yudao-starter-biz-data-permission**：
  - 基于部门/用户的数据权限
  - SQL级别过滤
  - 自定义权限规则

- **yudao-starter-biz-ip**：
  - IP地理位置解析
  - IP黑白名单
  - 访问统计

---

## 业务模块说明

### 1. yudao-module-system（系统管理）

**功能**：系统管理核心模块

**核心功能**：
- 用户管理：用户CRUD、密码管理、状态管理
- 角色管理：角色分配、权限配置
- 菜单管理：菜单树、路由配置
- 部门管理：组织架构树
- 字典管理：数据字典、字典类型
- 通知公告：站内信、邮件通知
- 短信管理：短信模板、发送记录
- 邮件管理：邮件模板、发送记录
- OAuth2：第三方登录（微信、QQ等）
- 操作日志：用户操作记录
- 登录日志：登录历史记录

**依赖关系**：
```
内部依赖：yudao-module-infra
框架依赖：
├── yudao-starter-security（安全认证）
├── yudao-starter-biz-data-permission（数据权限）
├── yudao-starter-biz-tenant（多租户）
├── yudao-starter-redis（缓存）
├── yudao-starter-mybatis（数据库）
├── yudao-starter-job（定时任务）
├── yudao-starter-mq（消息队列）
└── yudao-starter-excel（Excel导入导出）
```

**API接口**：
- `api/user` - 用户管理API
- `api/permission` - 权限管理API
- `api/dept` - 部门管理API
- `api/notify` - 通知管理API

### 2. yudao-module-infra（基础设施）

**功能**：基础设施和研发工具

**核心功能**：
- 代码生成：基于表结构生成CRUD代码
- 配置管理：动态配置、参数配置
- 文件存储：本地、FTP、SFTP、S3、OSS
- WebSocket：实时消息推送
- 服务器监控：服务器信息、性能指标
- API文档：Swagger管理
- 数据源管理：数据源配置

**依赖关系**：
```
功能依赖：
├── yudao-starter-biz-tenant（多租户）
├── yudao-starter-websocket（WebSocket）
├── yudao-starter-security（安全认证）
├── yudao-starter-mybatis（数据库）
└── yudao-starter-redis（缓存）
```

**API接口**：
- `api/file` - 文件管理API
- `api/config` - 配置管理API
- `api/codegen` - 代码生成API

### 3. 可选业务模块

#### yudao-module-bpm（工作流）
- 基于Flowable工作流引擎
- 流程定义、流程实例、任务管理
- 表单设计器、流程监控

#### yudao-module-pay（支付）
- 支付应用管理、商户管理
- 支付订单、退款订单
- 支付渠道：微信、支付宝、支付宝

#### yudao-module-mall（商城）
- 商品管理、订单管理
- 促销活动、优惠券
- 交易统计、数据分析

#### yudao-module-crm（客户关系管理）
- 客户管理、线索管理
- 商机管理、合同管理
- 客户跟进、业绩统计

#### yudao-module-erp（企业资源规划）
- 采购管理、销售管理
- 库存管理、财务管理
- 生产管理

#### yudao-module-ai（AI大模型）
- AI对话、AI绘画
- 提示词管理、API密钥管理
- 聊天记录

---

## 模块依赖关系

### 整体架构图

```
┌─────────────────────────────────────────────────────────┐
│                     yudao-server                         │
│                  主应用入口（端口48080）                  │
│                 聚合所有业务模块                         │
└─────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────┐
│                 业务模块（Business Modules）              │
│                                                          │
│  ┌────────────────────┐      ┌─────────────────────┐   │
│  │ module-system      │◄─────┤  module-infra       │   │
│  │ （系统管理）        │      │  （基础设施）        │   │
│  └────────────────────┘      └─────────────────────┘   │
│                                                          │
│  ┌─────────────────────────────────────────────────┐   │
│  │        可选模块（按需启用）                      │   │
│  │  bpm  │  pay  │  mall  │  crm  │  erp  │  ai   │   │
│  └─────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────┐
│            Framework Starters（技术组件）                │
│                                                          │
│  ┌──────┐  ┌──────────┐  ┌──────────┐  ┌──────┐       │
│  │ web  │→ │ security │→ │ mybatis  │→ │redis │       │
│  └──────┘  └──────────┘  └──────────┘  └──────┘       │
│                                                          │
│  ┌──────────┐  ┌──────────────┐  ┌─────────┐          │
│  │   job    │  │ data-permission│ │ tenant  │          │
│  └──────────┘  └──────────────┘  └─────────┘          │
└─────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────┐
│                   yudao-common                           │
│                   通用工具类、枚举、常量                  │
└─────────────────────────────────────────────────────────┘
```

### Framework Starters 依赖链

```
yudao-common（无依赖，最底层）
    ↓
yudao-starter-web
    ↓
yudao-starter-security
    ↓
    ├──→ yudao-starter-biz-data-permission
    └──→ yudao-starter-biz-ip
    ↓
yudao-starter-mybatis
    ↓
    ├──→ yudao-starter-biz-tenant
    └──→ yudao-starter-redis
    ↓
yudao-starter-mq
```

### Business Modules 依赖关系

#### yudao-module-system

```
yudao-module-system
│
├── 内部依赖
│   └── yudao-module-infra
│
└── 框架依赖
    ├── yudao-starter-security
    ├── yudao-starter-biz-data-permission
    ├── yudao-starter-biz-tenant
    ├── yudao-starter-mybatis
    ├── yudao-starter-redis
    ├── yudao-starter-job
    ├── yudao-starter-mq
    └── yudao-starter-excel
```

#### yudao-module-infra

```
yudao-module-infra
│
└── 框架依赖
    ├── yudao-starter-security
    ├── yudao-starter-biz-tenant
    ├── yudao-starter-websocket
    ├── yudao-starter-mybatis
    └── yudao-starter-redis
```

#### 可选业务模块（统一模式）

```
yudao-module-{bpm/pay/mall/crm/erp/ai}
│
└── 框架依赖（统一）
    ├── yudao-starter-web
    ├── yudao-starter-security
    ├── yudao-starter-mybatis
    └── yudao-starter-redis
```

---

## 模块间通信方式

### 1. API 接口模式

业务模块通过 `api/` 包暴露接口供其他模块调用。

**System 模块提供的 API**：

| API包 | 功能 | 主要接口 |
|-------|------|----------|
| `api/user` | 用户管理 | getUser(), getUserList(), validateUser() |
| `api/permission` | 权限管理 | hasPermission(), getPermissionList() |
| `api/dept` | 部门管理 | getDept(), getDeptList() |
| `api/notify` | 通知管理 | sendNotify() |

**Infra 模块提供的 API**：

| API包 | 功能 | 主要接口 |
|-------|------|----------|
| `api/file` | 文件管理 | createFile(), getFile() |
| `api/config` | 配置管理 | getConfig(), setConfig() |
| `api/codegen` | 代码生成 | generateCode() |

### 2. 依赖注入方式

```java
// 在其他模块中注入 System 模块的 API
@Service
public class MyService {

    @Autowired
    private UserApi userApi;

    @Autowired
    private PermissionApi permissionApi;

    public void doSomething(Long userId) {
        // 调用 API
        AdminUserDTO user = userApi.getUser(userId);
        boolean hasPermission = permissionApi.hasPermission(userId, "system:user:create");
    }
}
```

### 3. 跨模块调用流程

```
┌─────────────────┐         ┌─────────────────┐
│  调用方模块      │         │  提供方模块      │
│  (如 mall)      │         │  (如 system)    │
└─────────────────┘         └─────────────────┘
         │                            │
         │  1. 注入 API 接口           │
         │     @Autowired             │
         │     UserApi userApi;       │
         │                            │
         │  2. 调用 API 方法           │
         ├──────────────────────────→│
         │  userApi.getUser(id)      │
         │                            │
         │                            │  3. API 实现
         │                            │     (UserApiImpl)
         │                            │
         │                            │  4. 调用 Service
         │                            │     userService
         │                            │
         │  5. 返回结果               │
         │←──────────────────────────┤
         │                            │
         │  6. 处理结果               │
         │                            │
└─────────────────┘         └─────────────────┘
```

---

## 核心设计特点

### 1. 高度模块化

- **14个技术组件**：每个starter职责单一
- **多个业务模块**：系统管理、基础设施、工作流、支付、商城等
- **模块独立**：可单独使用，也可组合使用

### 2. 松耦合

- **模块间通过API接口通信**：避免直接依赖实现类
- **依赖倒置**：高层模块不依赖低层模块，都依赖抽象
- **接口隔离**：每个API接口职责明确

### 3. 可插拔

- **可选模块默认关闭**：在pom.xml中注释掉
- **按需启用**：需要时取消注释即可
- **编译时排除**：不启用的模块不会被编译

### 4. 分层清晰

```
应用层（Application Layer）
    └── yudao-server（聚合器）
业务层（Business Layer）
    └── yudao-module-*（业务模块）
框架层（Framework Layer）
    └── yudao-framework（技术组件）
基础层（Infrastructure Layer）
    └── yudao-common（通用工具）
```

### 5. 标准化

**统一包结构**：

```
yudao-module-{name}/
├── api/                 # API接口
│   └── {domain}/        # 领域API
├── controller/          # REST控制器
│   └── {domain}/        # 领域控制器
├── convert/            # 对象转换器
├── dal/                # 数据访问层
│   ├── dataobject/     # 数据对象(DO)
│   ├── mysql/          # MyBatis Mapper
│   └── redis/          # Redis操作
├── service/            # 业务逻辑层
│   └── {domain}/       # 领域服务
├── frameworks/         # 模块配置
├── mq/                 # 消息队列
├── job/                # 定时任务
└── enums/              # 枚举
```

### 6. 多租户支持

- 基于 `tenant_id` 的租户隔离
- 自动传递租户上下文
- 数据库层面自动过滤
- Redis缓存key隔离

### 7. 数据权限

- 基于部门/用户的数据权限
- SQL级别自动过滤
- 自定义权限规则
- 支持复杂权限场景

---

## 如何启用可选模块

### 步骤1：启用根模块

编辑根目录 `pom.xml`：

```xml
<modules>
    <!-- 核心模块 -->
    <module>yudao-dependencies</module>
    <module>yudao-framework</module>
    <module>yudao-server</module>
    <module>yudao-module-system</module>
    <module>yudao-module-infra</module>

    <!-- 可选模块 - 取消注释以启用 -->
    <module>yudao-module-bpm</module>
    <module>yudao-module-pay</module>
    <module>yudao-module-mall</module>
    <module>yudao-module-crm</module>
    <module>yudao-module-erp</module>
    <module>yudao-module-member</module>
    <module>yudao-module-ai</module>
    <module>yudao-module-iot</module>
</modules>
```

### 步骤2：添加到 yudao-server

编辑 `yudao-server/pom.xml`：

```xml
<dependencies>
    <!-- 核心模块 -->
    <dependency>
        <groupId>cn.iocoder.boot</groupId>
        <artifactId>yudao-module-system-biz</artifactId>
    </dependency>
    <dependency>
        <groupId>cn.iocoder.boot</groupId>
        <artifactId>yudao-module-infra-biz</artifactId>
    </dependency>

    <!-- 可选模块 - 取消注释以启用 -->
    <dependency>
        <groupId>cn.iocoder.boot</groupId>
        <artifactId>yudao-module-bpm-biz</artifactId>
    </dependency>
    <dependency>
        <groupId>cn.iocoder.boot</groupId>
        <artifactId>yudao-module-pay-biz</artifactId>
    </dependency>
    <dependency>
        <groupId>cn.iocoder.boot</groupId>
        <artifactId>yudao-module-mall-biz</artifactId>
    </dependency>
    <!-- 其他模块... -->
</dependencies>
```

### 步骤3：重新编译

```bash
# 清理并重新编译
mvn clean compile

# 或者跳过测试
mvn clean compile -DskipTests
```

### 步骤4：验证启动

```bash
# 启动应用
mvn spring-boot:run -pl yudao-server

# 或者运行jar
java -jar yudao-server/target/yudao-server.jar
```

### 注意事项

1. **数据库表**：部分模块需要初始化数据库表，参考 `sql/mysql/` 目录
2. **Redis**：确保Redis服务正常运行
3. **端口冲突**：默认端口48080，如有冲突请修改配置
4. **内存要求**：建议至少4GB内存
5. **模块依赖**：某些模块可能有依赖关系，按顺序启用

---

## 常见问题

### Q1: 为什么可选模块默认注释？

**A**: 为了减少编译时间和依赖复杂度。大部分场景下不需要所有模块，按需启用可以提高开发效率。

### Q2: 如何判断一个模块是否依赖另一个模块？

**A**: 查看 `pom.xml` 文件：
- 在模块的 `pom.xml` 中查看 `<dependencies>` 部分
- 如果依赖了另一个模块的 `api` 包，说明需要该模块

### Q3: 可以自定义模块吗？

**A**: 可以。参考现有模块的包结构，创建新的 `yudao-module-{name}` 模块：
1. 在根 `pom.xml` 中添加模块
2. 创建标准的包结构
3. 在 `yudao-server/pom.xml` 中添加依赖

### Q4: 模块间如何通信？

**A**: 三种方式：
1. **API接口**（推荐）：通过 `api/` 包定义接口
2. **消息队列**：异步通信，解耦模块
3. **数据库**：共享数据表（不推荐）

### Q5: 如何调试模块依赖问题？

**A**:
1. 使用 `mvn dependency:tree` 查看依赖树
2. 检查 `pom.xml` 中的依赖版本冲突
3. 查看 IDE 的依赖关系图
4. 运行 `mvn clean compile` 检查编译错误

---

## 参考资料

- **官方文档**：https://doc.iocoder.cn/
- **API文档**：http://localhost:48080/swagger-ui
- **源码仓库**：https://github.com/YunaiV/ruoyi-vue-pro

---

## 更新日志

- **2026-01-09**: 初始版本，整理模块结构和依赖关系
- 项目版本：`2026.01-SNAPSHOT`
- Spring Boot版本：3.5.9
- Java版本：17

---

> 文档生成时间：2026-03-09
> 如有疑问，请参考官方文档或提Issue
